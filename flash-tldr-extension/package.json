{
  "name": "flash-tldr-extension",
  "version": "1.0.0",
  "description": "AI-powered content summarizer browser extension",
  "main": "index.js",
  "scripts": {
    "icons": "python3 scripts/generate-icons.py",
    "build": "npm run icons && webpack --mode=production",
    "dev": "npm run icons && webpack --mode=development --watch",
    "pack": "npm run build && zip -r flash-tldr-extension.zip dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["chrome-extension", "ai", "summarizer", "productivity"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.0.0",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0",
    "ts-loader": "^9.4.0",
    "copy-webpack-plugin": "^11.0.0",
    "css-loader": "^6.8.0",
    "style-loader": "^3.3.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.4.4",
    "openai": "^4.0.0"
  }
}